---
paths:
    - "config/fish/**"
    - "tag-*/config/fish/**"
---

Test syntax: `fish --no-execute <file>`
Test config loads: `fish -c 'exit'`

Use fish idioms, not POSIX patterns. Fish is not POSIX-compatible by design.
